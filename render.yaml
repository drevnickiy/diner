services:
  - type: web
    name: diner-backend
    plan: free
    env: python
    buildCommand: pip install -r requirements.txt && python3 main.py --preview
    startCommand: python3 server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: DATABASE_URL
        sync: false
